<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FileSystemForensics.Views.Calendar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileSystemForensics.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Padding="20" Spacing="20">

        <TextBlock Margin="35 0 0 0"
                   FontSize="16">Select watcher</TextBlock>
        <ComboBox Margin="35 0 0 0"
                  x:Name="WatcherSelectionCombo"
                  ItemsSource="{x:Bind WatcherGuids}"
                  SelectedIndex="0"
                  SelectionChanged="WatcherSelectionCombo_SelectionChanged"
                  >

        </ComboBox>
        <CalendarView CalendarViewDayItemChanging="Calendar_CalendarViewDayItemChanging"
                      x:Name="FilesCalendar"
                      Width="800"
                      Height="500"
                      Margin="35 0 0 0"
                      SelectedDatesChanged="Calendar_SelectedDateChanged"
                      />
    </StackPanel>

</Page>
